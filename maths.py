import math
pi = -3.14

x = 3
y=5
z=7

#print(round(pi)) #Reduce the digits
#print(math.ceil(pi)) #Only number #Returns the smallest integral value
#print(math.floor(pi)) #Returns the closest integral value
#print(abs(pi)) #Returns the absolute value
#print(pow(pi, 2)) #
print(max(x,y,z)) #Returns the high value

